说明：
    此项目文件夹，用于将常用的模块包，进行打包并发布。供其他客户端，安装使用此安装包。

 项目说明：
    yolov8_onnx_export：原始onnx模型导出模块
    onnx_to_rknn_export：原始rknn模型量化模块

    1.加密版安装包动态库编译：用于创建不同模块加密动态库的项目
        核心py程序要改名为pyx
        注意：加密时使用Cython进行。进入到指定项目目录中，执行如下加密编译指令：且编译的动态库，要符合安装环境下，python的版本要求，如果不符合的话，要在编译环境下创建相同版本的python环境，然后进行编译。
            conda activate py312
            python setup_cython.py build_ext --inplace

    2.加密版发布安装包编译: 最终生成加密安装包的项目文件：
            注意：要删除不必要的相关文件。防止文件泄密
            删除所有源码： rm *.pyx *.c build/
